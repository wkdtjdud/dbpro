<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
    <script src="script.js"></script>
</head>
<body>
    
    <%-include('header.ejs')%>

    <div class="image-grid" style="display: grid; grid-template-columns: repeat(4, 1fr); ">
        <% participants.forEach(function(participant) { %>
            <div class="image-item" style=".img-resize:hover :transform: scale(1.1); /* 조금 확대된 효과 */
            cursor: pointer; text-align: center; "  >
        <img src="<%= participant.IMAGE %>" alt="<%= participant.PARTICIPANTNAME %>의 이미지" style="width: 170px; height: auto;" >
       
    <h3><%= participant.PARTICIPANTNAME %></h3>
</div>
    <% }) %>
  </div>
  <hr>
  <footer style=" display: flex; text-align: center; justify-content: center; align-items: center; ">
    <div class="profile" style="width: 50px; height: 50px; border-radius: 50%; "></div> 
<button style="cursor: pointer; width: 200px; height: 45px;">투표하기</button>
</br>
</footer>


</body>
</html>